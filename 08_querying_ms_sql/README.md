# 08 — Querying MS SQL

SQL kullanarak veri sorgulama.

## Konular (Topics)

- **SELECT queries** - SELECT sorguları
- **WHERE conditions** - WHERE koşulları
- **GROUP BY** - GROUP BY ifadesi
- **HAVING** - HAVING ifadesi
- **JOIN operations** - JOIN işlemleri
- **Subqueries** - Alt sorgular
- **Window functions** - Pencere fonksiyonları
- **Analytical queries** - Analitik sorgular

## Klasör Yapısı

```
08_querying_ms_sql/
├── exercises/          # Pratik alıştırmalar
├── notebooks/          # Jupyter çalışmalar
├── projects/           # Küçük uygulamalar
└── README.md
```

## Öğrenme Hedefleri

Bu modülü tamamladıktan sonra, MS SQL Server kullanarak veritabanlarından veri sorgulayabilecek, kompleks analizler yapabilecek ve veri altyapısını etkin bir şekilde kullanabileceksiniz.
